=== Working with the terminal

Under the hood of MacOS is a Linux operating system. You can access it by opening the http://www.wikihow.com/Open-Applications-Using-Terminal-on-Mac[terminal]. You can open a terminal window unsing Finder. Once you open a terminal window you will see the `$` symbol which indicates a command line waiting for commands, just like in Linux. 

